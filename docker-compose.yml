version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: coldbot-postgres
    environment:
      POSTGRES_DB: coldbot
      POSTGRES_USER: coldbot
      POSTGRES_PASSWORD: coldbot_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U coldbot"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
